version: '3.9'
services:
  db:
    build:
      context: ./db
    volumes:
      - ./db:/db
    restart: on-failure:5
    ports:
      - '${DB_PORT}:${DB_PORT}'
    environment:
      POSTGRES_USER: 'test'
      POSTGRES_PASSWORD: 'test'
      POSTGRES_DB: 'test'
  server:
    build:
      context: ./
    volumes:
      - ./log:/app/log
      - ./build:/app/build
    depends_on:
      - db
    restart: on-failure:5
    ports:
      - '${PORT}:${PORT}'
networks:
  default:
    name: rs-net
